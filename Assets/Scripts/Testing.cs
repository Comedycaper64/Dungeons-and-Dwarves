using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testing : MonoBehaviour
{
    [SerializeField] private Unit unit;

    //Scripts for if anything needs testing
    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.T))
        {
        
        }

    }

}
